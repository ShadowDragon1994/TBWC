export type CustomerIntent = { purpose:'gift'|'self-use'|'unknown';recipient:string;holiday:string;budgetMax:number|null;destination:string;deadline:string;customization:{engraving:string;giftWrap:boolean};requiresConfirmation:boolean;risks:string[];autoSendAllowed:boolean;confidence:number }
async function post<T>(path:string,body:unknown):Promise<T>{const response=await fetch(path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});if(!response.ok)throw new Error((await response.json().catch(()=>({}))).message||'客服操作失败');return response.json()}
export const customerServiceApi={analyze:(message:string)=>post<{data:{intent:CustomerIntent;reply:string}}>('/api/customer-service/analyze',{message}),send:(message:string)=>post<{data:{status:string}}>('/api/automation/executions',{adapterId:'mock',capability:'xiaohongshu.customer-service.send',payload:{conversationId:'mock-conversation',message}})}
