# 个人电商内容工作台 PRD

## 1. 摘要

这是一款供个人运营者使用的本地优先工具。用户录入一份商品资料后，工具协助完成文案生成、图片方案、合规检查、模板排版和多尺寸导出。

首版不追求无人值守，也不建设多个复杂 Agent。它只解决一个高频问题：把重复的商品内容制作流程压缩成一个可检查、可修改、可复用的工作流。

## 2. 联系人与责任

| 角色 | 负责人 | 责任 |
|---|---|---|
| 产品负责人 | 用户本人 | 决定范围、验收结果 |
| 运营与审核 | 用户本人 | 提供商品资料、确认内容、发布商品 |
| 开发与维护 | 待定 | 实现、部署和维护工具 |
| AI 服务 | 可配置 | 文案生成、图片生成、内容检查 |

个人版不建设组织、部门、审批人和多租户权限。所有高风险操作由用户本人确认。

## 3. 背景

个人运营文创与节庆商品时，需要在商品资料、文案工具、绘图工具、Photoshop 和平台后台之间反复切换。主要问题是：

- 同一份商品信息需要重复复制和改写。
- 主图、详情图和社媒图需要重复调整尺寸与文字。
- AI 输出分散，好的版本难以保存和复用。
- 极限词、虚假功效、字体和图片授权容易遗漏。
- 复杂的企业级 Agent 系统开发和维护成本过高。

现在适合先做个人工具，因为大模型、图片生成和模板脚本已经能处理部分重复工作，但最终质量和发布风险仍需要用户判断。

## 4. 目标

### 4.1 产品目标

让一个人可以在一个工作台内完成“资料整理—内容生成—人工修改—风险检查—模板导出”的闭环。

### 4.2 关键结果

- 对固定的 20 个真实商品样本，80% 能完成全流程导出。
- 单个商品从资料录入到得到首版素材不超过 10 分钟。
- 用户实际操作时间相较现有流程减少 50%。
- 已知敏感词测试集召回率不低于 99%。
- PSD 固定模板批量导出成功率不低于 95%。
- 每次模型调用都展示或记录模型、耗时和估算成本。

### 4.3 非目标

首版不包含：

- 自动抓取小红书、抖音等平台。
- 自动判断爆款和自动选品。
- 自动客服、自动议价或退款处理。
- ERP 库存预测和自动采购。
- 自动改价、自动上架和自动发布。
- 任意图片自动拆成专业可编辑图层。
- 多人协作、复杂权限与审批流。

## 5. 使用场景

### 5.1 目标用户

独立经营淘宝文创、礼品、定制或节庆商品，同时承担选品、内容、设计和运营工作的人。

### 5.2 核心任务

1. 将供应商资料整理成统一的商品档案。
2. 快速生成可修改的标题、卖点和详情文案。
3. 生成或组合商品场景图。
4. 检查敏感词、夸大表达、AI 标识和素材授权。
5. 套用固定模板并导出多个渠道尺寸。
6. 保存采用过的内容，后续换节日或换渠道时复用。

### 5.3 约束

- 使用者只有一人，操作必须简单。
- AI 和外部平台可能不稳定或产生费用。
- 商品真实性、版权和最终发布责任仍由用户承担。
- Photoshop 自动化可能依赖本机安装的 Photoshop。

## 6. 价值主张

### 6.1 用户获得什么

- 少复制：商品资料只录一次。
- 少切换：生成、修改、检查和导出集中在一个工具中。
- 少返工：固定模板和品牌规则可以复用。
- 更可控：AI 不直接发布，所有结果先预览再确认。
- 可追溯：保留提示词、结果、修改、导出和费用记录。

### 6.2 产品原则

- 工作流优先于聊天框。
- 模板优先于自由生成。
- 局部重生成优先于整页推倒重来。
- 用户确认优先于自动执行。
- 本地保存优先，云服务按需调用。
- 一个主流程优先于多个拟人化 Agent。

## 7. 解决方案

### 7.1 产品形态

建议采用桌面端或本地 Web 应用。工具在电脑本地运行，通过浏览器访问；商品资料、配置和操作记录默认保存在本地数据库，图片和 PSD 保存在用户选择的目录。

主导航只保留五项：

1. 今日工作台
2. 商品库
3. 创作任务
4. 模板与素材
5. 设置

### 7.2 核心流程

```text
新建/选择商品
  → 补齐商品资料
  → 选择节日、渠道和模板
  → 生成标题、卖点、详情文案和视觉方案
  → 用户逐项采用、修改或重生成
  → 自动风险检查
  → 用户确认
  → 写入模板并导出
  → 保存为一个可复用版本
```

任务状态：

```text
草稿 → 生成中 → 待编辑 → 待检查 → 待确认 → 导出中 → 已完成
```

异常状态：`生成失败`、`检查未通过`、`模板错误`、`导出失败`。

### 7.3 页面设计

#### 今日工作台

- “新建创作任务”作为主按钮。
- 显示最近商品、未完成任务和失败任务。
- 显示本月生成次数、费用估算和节省时间。
- 提醒即将到来的节日，但不自动创建任务。

#### 商品库

每个商品包含：

- 基础信息：名称、类目、价格、材质、尺寸、颜色。
- 供应信息：供应商、链接、成本、交期。
- 营销信息：人群、场景、卖点、品牌语气、禁用表达。
- 媒体资料：原图、Logo、参考图、授权凭证。
- 历史版本：采用过的标题、图片和导出记录。

支持从表格粘贴资料，但首版不强求自动同步 1688。

#### 创作任务

界面采用三栏布局：

- 左侧：商品资料、渠道、节日、风格和模板。
- 中间：主图/详情图预览画布。
- 右侧：标题、卖点、合规风险和生成控制。

用户可以锁定满意的图片或文案，只重生成某个局部。每次生成 3 个候选，避免一次产生大量无用结果。

#### 检查与确认

风险分为三级：

- 红色：禁止导出，例如缺少 AI 标识、命中明确禁用词。
- 黄色：允许确认后继续，例如可能夸大、授权信息不完整。
- 蓝色：优化建议，例如标题过长、卖点重复。

每条风险必须展示位置、原因、建议改法和处理状态。用户可修改、忽略或加入个人白名单。

#### 模板与素材

- 管理固定 PSD 模板、品牌色、Logo、字体和常用图标。
- 每个模板定义标准占位符，而非操作任意图层。
- 展示字体和素材的授权状态。
- 支持一键测试模板，提前发现缺失字体和文字溢出。

#### 设置

- 配置文本模型和图片模型。
- 配置 API 密钥，密钥只保存在本机安全存储中。
- 设置单次和每月预算提醒。
- 管理个人敏感词、品牌规则和输出目录。
- 导入、导出和备份本地数据。

### 7.4 功能模块

#### A. 商品资料助手

- 检查缺失字段。
- 从粘贴文本中提取材质、尺寸和卖点候选。
- 不确定的信息必须标记“待确认”，不得自行补全为事实。

#### B. 文案生成

- 生成淘宝标题、主图短文案、详情页卖点和社媒文案。
- 生成时引用结构化商品字段，避免只依赖长提示词。
- 支持“更简短”“更节日化”“保留事实重写”等局部操作。
- 显示内容依据；商品资料中不存在的事实自动提示。

#### C. 视觉制作

- 首版以商品原图加生成背景为主，避免 AI 重造商品主体。
- 支持主体抠图、背景生成、尺寸裁剪和简单文字排版。
- 所有生成图片保留来源、模型和生成时间。

#### D. 风险检查

- 本地规则检查：敏感词、极限词、长度、联系方式。
- AI 语义检查：夸大宣传、错误功效、误导性对比。
- 授权检查：所用字体、图片、Logo 是否有授权记录。
- AI 标识检查：需要标识的导出物是否已包含标识。

检查结果是辅助判断，不宣称替代法律或平台审核。

#### E. 模板排版与导出

PSD 模板使用统一命名：

- `PRODUCT_IMAGE`
- `BACKGROUND`
- `TITLE`
- `SUBTITLE`
- `SELLING_POINT_1`
- `PRICE`
- `LOGO`
- `AI_LABEL`

脚本负责替换智能对象和文本、检测溢出、导出 800×800、750px 宽详情图和 3:4 素材。出现字体缺失、文本溢出或图层缺失时停止导出并提示用户。

如果 Photoshop 自动化不稳定，首版可以先使用 HTML/Canvas 模板导出 PNG，把 PSD 输出放入后续版本。

#### F. 历史与复用

- 保存每个商品的创作版本。
- 可以复制旧任务并更换节日、渠道或模板。
- 标记“已采用”结果，形成个人偏好库。
- 支持导出一个包含图片、文案和检查报告的素材包。

### 7.5 数据结构

首版至少包含以下实体：

- 商品 `Product`
- 商品图片 `ProductAsset`
- 创作任务 `CreativeTask`
- 生成结果 `Generation`
- 内容版本 `ContentVersion`
- 风险结果 `ComplianceFinding`
- 模板 `Template`
- 导出记录 `ExportJob`
- 模型费用记录 `UsageRecord`

图片和 PSD 存文件目录，数据库只保存路径和元数据，避免个人版数据库过大。

### 7.6 技术建议

- 前端：本地 Web 界面，优先选择开发者熟悉的框架。
- 后端：单体应用，不拆微服务。
- 数据：SQLite。
- 文件：本地项目目录，可手动备份。
- 任务：本地队列处理生成和导出，不引入复杂消息系统。
- AI：通过统一适配层连接一个文本模型和一个图片模型。
- PS：先验证 UXP/脚本方案；不可控时使用模板渲染作为降级方案。
- 安全：API 密钥不写入日志，不随项目备份明文导出。

### 7.7 关键假设

- 用户愿意在发布前逐项确认内容。
- 固定模板可以覆盖大多数日常商品图。
- 商品原图质量足以支持抠图和合成。
- 个人使用不需要复杂账号体系。
- 本地数据和手动备份能满足首版需求。
- 文案和图片模型成本低于节省的人工作业成本。

这些假设需要通过真实样本验证，不能直接当作事实。

## 8. 发布计划

### 阶段 0：验证，约 1 周

- 准备 20 个真实商品和 3 个常用模板。
- 完成文本生成、合规规则和模板导出三个小实验。
- 记录采用率、耗时、费用和失败原因。
- 根据结果决定首版采用 PSD 还是普通图片模板。

### 阶段 1：可用原型，约 2 至 3 周

- 商品库。
- 创建创作任务。
- 文案候选生成与局部修改。
- 基础敏感词检查。
- 固定模板预览与图片导出。
- 本地历史记录。

### 阶段 2：个人正式版，约 2 至 4 周

- 图片背景生成和主体合成。
- 完整风险检查。
- PSD 自动化或稳定的替代方案。
- 多尺寸批量导出。
- 费用统计、备份和恢复。

### 后续候选能力

只有在首版每周稳定使用后再考虑：

- 淘宝草稿箱对接。
- 1688 商品资料导入。
- 节日批量换肤。
- 根据已采用内容学习个人偏好。
- 简单的客服文案辅助。

不因“看起来智能”而增加独立 Agent。新增功能必须能减少明确的操作时间或错误率。

## 附录：首版验收场景

选择一个真实商品，录入商品资料和原始图片，选择“七夕”主题及淘宝主图模板。工具应当：

1. 生成 3 个标题和 3 组卖点。
2. 允许锁定标题并单独重生成卖点。
3. 生成或选择一个不改变商品主体的场景背景。
4. 标记极限词、未知事实和缺少授权记录的素材。
5. 经用户确认后填充模板。
6. 输出主图、详情图、3:4 素材及一份文案文件。
7. 保存本次采用内容、风险处理和费用记录。

上述流程能在 10 分钟内完成，且失败后可以从当前步骤继续，视为首版核心闭环通过。
